CREATE TABLE `gbentry` (
  `id` INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  `fullName` TEXT NOT NULL,
  `emailAddress` TEXT NOT NULL,
  `website` TEXT NULL,
  `comment` TEXT NULL,
  `timestamp` TEXT NOT NULL
  );
